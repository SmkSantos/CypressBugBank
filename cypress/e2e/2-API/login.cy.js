/// <reference types="cypress" />

context('LOGIN - API', () => {
  beforeEach(() => {
    cy.visit('')
  })

  // https://on.cypress.io/interacting-with-elements

  it('Login via API', () => {
  })
})
